# Prospective capture data is research-critical raw provenance, captured at
# runtime by src/research/prospective/cli.py. It can grow large and must never
# be committed. Only this .gitignore and small example artifacts are tracked.
*
!.gitignore
!examples/
!examples/**

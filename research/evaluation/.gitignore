# Only small evaluation artifacts (reports, small json summaries) are tracked.
# Large dumps (>1MB) produced by evaluation runs must not be committed.
*.jsonl
*.jsonl.gz
*.parquet
*.csv
prospective_capture_dump*

"""Correlation_Structure constants and implied-vs-measured comparison.

Responsibility:
    Encode the empirically measured near-zero cross-market correlation constants
    (cards x corners -0.033, cards x goals -0.030, corners x goals -0.028, 95% CI
    approximately +/-0.016), compute the correlation implied by the per-side
    model, and red-flag material deviation while always reporting the measured
    values and their confidence intervals alongside the comparison.

Scaffold only — the correlation comparison is implemented in task 6.2.
"""

"""Reporting — complete, honest headline report assembly.

Responsibility:
    Assemble headline per-side-vs-baseline results per market and per league, the
    Rich vs Broad corpus comparison, readable elastic-net coefficients per
    dimension, ECE and reliability curves per target, out-of-sample BSS vs a naive
    baseline, Brier and ECE, the fresh FDR family size, and a confidence interval
    for every reported estimate. Reports all results including failures with no
    post-hoc selection, and labels any estimate whose CI spans zero as "not a
    result".

Scaffold only — reporting is implemented in task 10.
"""

"""Derived, read-only out-of-sample evaluation tooling for the champion model.

Nothing in this package mutates canonical evidence (corpus, commitments,
shadows, evaluations, settlements, frontier). It reads the champion training
corpus and produces derived metric artifacts under ``research/evaluation/``.
"""
